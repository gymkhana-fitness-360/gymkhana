"use strict";

/** Reminders cadence */
export const REMINDER_RULES = Object.freeze({
  DAYS_BEFORE_EXPIRY: Object.freeze([7, 3, 1]),
  DAYS_AFTER_EXPIRY: Object.freeze([1, 3, 7]),
  MAX_REMINDERS_PER_MONTH: 5,
});
