# Local data (not committed)

Place payment imports, spreadsheets, and other gym-specific files here.

This directory is **gitignored** so member PII never enters the repository.

Use `npm run import:payments` or scripts under `scripts/imports/` (e.g. `consolidate_payments.py`) for ingestion workflows.
