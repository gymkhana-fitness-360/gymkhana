{
  "preferred_patterns": [
    { "id": "thin-route", "example": "references/templates/api-route.ts", "message": "Auth + rate limit + parseJsonBody + domain handler" },
    { "id": "currency", "example": "formatCurrency() from @/lib/utils", "message": "All money display in product" },
    { "id": "tenant-query", "example": "where: { accountId, gymId }", "message": "Scope every tenant query" },
    { "id": "shadcn-button", "example": "@/components/ui/button", "message": "UI primitives from shadcn" },
    { "id": "domain-rules-constants", "example": "ATTENDANCE_RULES from @/domains/attendance/rules", "message": "No magic numbers in handlers — import rule slices" },
    { "id": "cron-verify", "example": "verifyCronRequest + startJob/completeJob", "message": "All /api/cron routes authenticated and tracked" },
    { "id": "undo-snapshot", "example": "playbooks/undo-destructive-action.md", "message": "Destructive admin delete → audit undoSnapshot + gymId" },
    { "id": "campaign-probe-draft-queue", "example": "domains/campaigns/service.ts", "message": "Configurable segment campaigns — not hardcoded cohort pages" },
    { "id": "admin-dry-run", "example": "playbooks/admin-maintenance-api.md", "message": "Bulk fix APIs support dryRun before execute" },
    { "id": "no-store-bootstrap", "example": "noStoreJson() from @/lib/api-cache", "message": "Attendance bootstrap responses never cached at CDN" }
  ]
}
